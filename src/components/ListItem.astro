---
import { Image } from "astro:assets";
interface Props {
  name: string;
  description: string;
  url: string;
  alt?: string;
  urlImage?: string;
}
const { name, description, url, alt, urlImage } = Astro.props;
---

<li>
  <a href={url} class="text-[#909090]">
    {name}
  </a>
  <p class="text-xs text-[#909090] mt-1">
    {description}
  </p>
  {
    urlImage && alt && (
      <div class="mt-2">
        <Image
          src={urlImage}
          alt={alt}
          width={350}
          height={200}
          class="rounded-md"
        />
      </div>
    )
  }
</li>
